# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in
this repository.

@AGENTS.md

**`AGENTS.md` (imported above) is the single source of truth for this project** —
commands, architecture, content pipeline, deployment and the constraints that matter.
Read it before writing code, and update it there when something changes.

Do not restate its contents here. This file used to carry an English copy of the same
facts; the two drifted apart and the copy went stale, which is why it is now a pointer.
